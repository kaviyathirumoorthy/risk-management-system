spring:
  application:
    name: risk-management-system

  datasource:
    url: jdbc:postgresql://localhost:5432/risk_management
    username: postgres
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true